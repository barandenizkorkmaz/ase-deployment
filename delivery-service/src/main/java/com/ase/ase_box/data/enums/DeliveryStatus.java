package com.ase.ase_box.data.enums;

public enum DeliveryStatus {
    DISPATCHED,
    // TODO: DISPATCHED: After dispatcher dispatched, but the deliverer didn't collect yet.
    SHIPPING,
    DELIVERED,
    COLLECTED
}
